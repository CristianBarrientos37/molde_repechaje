

<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Horarios</h2>
    <% if current_user&.user_type == 'administrator' %>
      <%= link_to "Nuevo Horario", new_schedule_path, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
